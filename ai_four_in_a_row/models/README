# Model checkpoints are saved here by train.py
# ai_four_in_a_row_model_iteration_current.pt   ← latest checkpoint (overwritten each save)
# ai_four_in_a_row_model_iteration_n.pt         ← versioned snapshots
#
# Run train.py to generate the first checkpoint before starting the API.
